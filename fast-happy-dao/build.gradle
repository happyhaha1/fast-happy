apply plugin: 'spring-boot'


dependencies {
    compile(
            "org.springframework.boot:spring-boot-starter-data-jpa",
            project(':fast-happy-model')
    )
    testCompile(
            "com.h2database:h2",
            'org.springframework.boot:spring-boot-starter-test'
    )
}
